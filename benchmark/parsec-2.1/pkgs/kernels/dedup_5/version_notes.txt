In hashtable.c hash(), delete the bit shifting and XOR the key chunk
Support external global lock and fine-grained lock (for HTM)
Add -DBALANCED to optimze the thread assignment
In queue.c, make the for loop into several in dequeue() and enqueue()
In encoder.c, move malloc out of TX in sub_Chunkprocess, move write_file out of TX
